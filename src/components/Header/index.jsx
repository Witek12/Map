import React from "react";
import { redirectTo } from "@reach/router";

const Header = () => {
return(
    <div class="Header">
        <h1>
            Interactive map.
        </h1>
    </div>
)
}

export default() => <Header />